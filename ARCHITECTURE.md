# 🏗️ Midas Financial Bot - Architecture Overview

## 1. Guiding Principles

- **Clean Architecture:** Разделение на слои (Domain, Application, Infrastructure) для слабой связанности и высокой тестируемости.
- **Domain-Driven Design (DDD):** Фокус на бизнес-логике и предметной области (транзакции, счета, категории).
- **SOLID:** Принципы проектирования для создания поддерживаемого и расширяемого кода.
- **Production-Ready:** Безопасность, надежность и наблюдаемость встроены в архитектуру.

## 2. Project Structure

```
midas-finance-bot/
├── src/
│   ├── domain/                 # Бизнес-логика и сущности
│   │   ├── entities/           # (User, Account, Transaction, Category)
│   │   ├── services/           # (balance_monitor, pattern_detector, ai_categorizer)
│   │   └── exceptions/         # Кастомные исключения
│   ├── application/            # Сценарии использования (Use Cases)
│   │   ├── use_cases/          # (add_transaction, get_report, etc.)
│   │   └── services/           # Оркестрация доменных сервисов
│   ├── infrastructure/         # Внешние зависимости
│   │   ├── database/           # (Repositories, Unit of Work, Supabase client)
│   │   ├── security/           # (Encryption, Rate Limiter, Input Validator)
│   │   ├── logging/            # (Audit Logger)
│   │   ├── telegram/           # (Bot handlers, keyboards)
│   │   ├── monitoring/         # (Health Check)
│   │   └── external_apis/      # (Claude client)
│   └── main.py                 # Точка входа, инициализация
├── tests/                      # Юнит и интеграционные тесты
│   ├── unit/
│   └── integration/
├── migrations/                 # Миграции базы данных (Alembic)
├── .github/workflows/main.yml  # CI/CD Pipeline
├── Dockerfile                  # Docker-контейнер для production
├── docker-compose.yml          # Оркестрация сервисов
└── README.md
```

## 3. Core Components

### Domain Layer

- **Entities:** Чистые Python-объекты, представляющие бизнес-сущности (`Transaction`, `Account`). Не зависят ни от чего.
- **Services:** Содержат основную бизнес-логику. `BalanceMonitor` и `PatternDetector` - сердце инновационной фичи определения транзакций.
- **Exceptions:** Кастомные исключения для точной обработки ошибок.

### Application Layer

- **Use Cases:** Реализуют конкретные сценарии, например, `AddTransactionUseCase`. Оркестрируют работу с репозиториями и доменными сервисами.
- **Services:** Сервисы уровня приложения, которые координируют несколько доменных сервисов.

### Infrastructure Layer

- **Database:**
  - **Repositories:** Реализуют интерфейсы для доступа к данным (например, `TransactionRepository`).
  - **Unit of Work:** Гарантирует атомарность операций с базой данных. Все изменения либо применяются, либо откатываются вместе.
  - **Supabase Client:** Взаимодействует с Supabase (PostgreSQL).

- **Security:**
  - **EncryptionService:** Шифрует чувствительные данные (например, API ключи пользователя) с помощью Fernet.
  - **InputValidator:** Проверяет все входящие данные с помощью Pydantic для предотвращения уязвимостей.
  - **RateLimiter:** Защищает от флуда и DoS-атак.

- **Telegram:**
  - **Handlers:** Обработчики команд и сообщений от пользователя.
  - **Keyboards:** Генерация inline-клавиатур.

- **Monitoring & Logging:**
  - **AuditLogger:** Записывает все важные действия в системе для последующего анализа.
  - **HealthCheck:** Endpoint для проверки состояния всех компонентов системы.
  - **GlobalExceptionHandler:** Централизованный обработчик ошибок.

## 4. Data Flow Example: Add Transaction

1. **Telegram Handler (`infrastructure/telegram/handlers`)**
   - Получает сообщение от пользователя.
   - Вызывает `InputValidator` для проверки данных.

2. **Use Case (`application/use_cases`)**
   - Запускает `UnitOfWork`.
   - Вызывает `TransactionRepository` для создания транзакции.
   - Вызывает `AccountRepository` для обновления баланса счета.
   - Вызывает `AICategorizer` для определения категории.
   - Коммитит `UnitOfWork`.

3. **Domain Services (`domain/services`)**
   - `AICategorizer` отправляет запрос к Claude API и возвращает категорию.

4. **Repositories (`infrastructure/database/repositories`)**
   - Выполняют SQL-запросы к базе данных.

5. **Telegram Handler**
   - Отправляет пользователю подтверждение об успешном добавлении транзакции.

## 5. Technology Stack

- **Python 3.11+**
- **aiogram 3.x:** Асинхронный фреймворк для Telegram ботов.
- **PostgreSQL (Supabase):** Надежная реляционная база данных.
- **Anthropic Claude 3.5 Sonnet:** Для AI-функций.
- **Pydantic:** Для валидации данных.
- **Alembic:** Для миграций базы данных.
- **Pytest:** Для тестирования.
- **Docker & Docker Compose:** Для контейнеризации и развертывания.

## 6. Security & Reliability

- **Изоляция зависимостей:** Clean Architecture предотвращает смешивание бизнес-логики с инфраструктурным кодом.
- **Атомарность:** Unit of Work гарантирует консистентность данных.
- **Наблюдаемость:** Health checks, аудирование и глобальная обработка ошибок позволяют быстро диагностировать и решать проблемы.
- **Безопасность:** Шифрование, валидация ввода и rate limiting встроены в архитектуру.
